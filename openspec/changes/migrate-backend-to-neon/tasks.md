## 1. Shared database foundation

- [x] 1.1 Reach the management system's Neon database from a Worker, implementing the decisions to run Cloudflare Workers with Hyperdrive as the API runtime and to use a shared Neon database with the management system. Verify by connecting with psql using that database's connection string, selecting a count from persons, and confirming the Hyperdrive binding is listed for the Worker.
- [x] 1.2 Deliver the relational schema for worship entities in `workers/migrations/0001_init.sql`, following the design decision on a Postgres schema mirroring sheet entities with real constraints: worship-prefixed tables for weeks, votes, schedule, songs, vote settings, and member profiles, with week identifiers stored as dates, foreign keys to the weeks table, and foreign keys from every member reference to the management system's persons table. Verify by applying the migration to a Neon branch and querying information_schema to assert each table, its primary key, and its foreign keys exist.
- [x] 1.3 Enforce the integrity rules in the same migration: one vote per member per week as a composite primary key, schedule role cardinality enforced by the schema through unique indexes allowing two rows for 主領 and 配唱 and one for every other role, and worship profile attributes stored with their natural types with at most one profile row per person. Verify with SQL statements that must fail — a second 鋼琴 assignment in one week, a duplicate vote, a second profile for one person — and one that must succeed.

## 2. Roster from the management system

- [x] 2.1 Serve the roster derived from the management system, implementing the decision on member directory read from the management system: a member is a person with an active 敬拜部 membership and an active person record, and each member's instruments sourced from team positions per the decision on instruments sourced from the 敬拜部 team positions, with canPPT derived from holding the PPT position. Verify by adding a test person to 敬拜部 with the 鋼琴 position in the management system, asserting they appear in getMembers with that instrument, adding PPT and asserting canPPT flips to true, then removing the test membership.
- [x] 2.2 Store worship-specific attributes in a worship-owned profile, implementing the decision on worship-specific member attributes in a worship-owned profile table, and serve a saveMemberProfile action for the in-app role, scheduling constraint, avatar color, initials, and the team's display name, the last added by `workers/migrations/0003_member_display_name.sql`. Verify that a person with no profile row appears in the roster under their formal name with the member role and no constraint, that a member with a display name is presented under it, that saving a constraint returns it on the next roster read, and that the person record in the management system is unchanged.
- [x] 2.3 Ensure management-owned tables are read-only in normal operation by retiring saveMember and deleteMember, and by reworking the SPA's member screen so church-record attributes — name of record, email, instruments, PPT eligibility, and team membership itself — are shown but not editable, with a link to the management system's admin pages, while the worship-only attributes remain editable through saveMemberProfile. Verify that each retired action returns an error envelope naming it as unsupported, that a full read and write pass — including a member profile save and a LINE bind — leaves every management-owned table byte-identical, and that the member screen has no control that adds, deletes, or edits a church-record attribute.

## 3. Data migration

- [x] 3.1 Produce a snapshot of current sheet data with `scripts/export-sheets.mjs`, reading the six CSV files exported from Google Sheets rather than the Apps Script actions so duplicate rows survive into the snapshot, implementing the design decision of a one-off export and import instead of dual-write. Verify by asserting the snapshot record count for each collection equals the CSV data row count, and by confirming a deliberately duplicated schedule row in the CSV is present twice in the snapshot.
- [x] 3.2 Resolve members matched to persons during migration with `scripts/match-persons.mjs`, applying email, exact normalized name, Chinese-name suffix, and English-name token rules in that order, stopping at the first rule with exactly one candidate, marking a member ambiguous when a rule yields several, and writing every unresolved member to a mapping file for human completion. Verify against the current 18 members and 51 persons by asserting the rules resolve 15 with zero ambiguous, that each resolution names the rule that produced it, that 世綸, 茜茜, and 阿蒝 are written to the mapping file as unresolved, and that a completed mapping file passes validation.
- [x] 3.3 Load a snapshot into Neon with `scripts/import-neon.mjs` so that migration from the spreadsheet validates before loading and member identifiers are person identifiers: the load runs in one transaction, rewrites every exported member reference through the completed mapping, and aborts with the offending rows named when a constraint is violated or a member is unmapped. The import renames the legacy role 弦樂 to Keyboard before validating, and honors a waiver file naming a week and role whose cardinality conflict is accepted last-row-wins. Verify by importing the current export with the completed mapping and a waiver for 2026-04-11 吉他, asserting table counts match the snapshot less the one waived drop, that all 11 弦樂 rows load as Keyboard, and that votes reference person UUIDs; then re-run with an unmapped member, with a duplicate 鼓 assignment, and with the waiver removed, asserting each aborts and leaves the database unchanged.
- [x] 3.4 Carry existing bindings across with `scripts/seed-line-identities.mjs`, delivering the one-off seed of existing LINE bindings into a worship-owned table added by `workers/migrations/0002_line_identities.sql`, per the decision that LINE bindings stay worship-owned, in worship's own id space. Verify that all 18 sheet bindings are seeded or reported, that a person already bound is left untouched, that an identifier already held by another person is refused and reported, that a second run creates no rows, and that the management system's identity table is unchanged throughout.

## 4. Worker API foundation

- [x] 4.1 Serve a health check action that executes the roster join through Hyperdrive and reports success, failing with a named relation when an upstream schema change removes a column it reads. Verify by requesting it against a local wrangler dev session, then against a branch where a joined column is renamed, confirming the failure envelope names the relation.
- [x] 4.2 Implement action routing over two entry points and the uniform response envelope, honoring the decision to keep the action-based request contract: reads dispatch from GET query parameters, writes from a JSON POST body, and every response is `{ok, data}` or `{ok, error}` with a JSON content type. Verify by dispatching one read and one write action and asserting an unrecognized action returns an error envelope naming that action.
- [x] 4.3 Gate every request behind shared secret authentication with a constant-time comparison, honoring the decision to keep the shared-secret gate, unchanged, for this migration, read every credential through credentials supplied by runtime bindings, and return cross-origin access for the published SPA on success and error responses alike. Verify that a wrong secret returns Unauthorized with no query issued, that an action whose binding is absent fails naming the missing configuration, and that both a successful and a rejected response carry the allow-origin header for the GitHub Pages origin.

## 5. Action parity

- [x] 5.1 Build `scripts/parity-check.mjs` so it replays a fixed set of read actions against both the Apps Script deployment and the Worker and reports a diff per action, normalizing both payloads first: member identifiers translated through the mapping file, scalars compared by value so "TRUE" equals true and "3" equals 3, empty string and null and absent treated alike, the fields no screen reads ignored — practice and service times, vote-opened stamps, and last-updated stamps, but not the vote deadline, which the vote screen shows — schedule rows naming the placeholder dash treated as absent, and collections keyed by their natural key rather than array order. Roster actions are compared only on the scheduling-relevant fields for people present on both sides, reporting one-sided people instead of failing. Verify by running it against actions the Worker does not implement yet and confirming they are reported as differing rather than passing vacuously, and by asserting the normalizer's own rules with a fixture pair that differs only in representation.
- [x] 5.2 Serve the week and vote-setting reads and writes so that response payloads preserve the current client contract, including the getInitialData bundle of members, weeks, and voteSettings plus mySchedule when a member id is supplied. Verify with the parity script that getWeeks, getWeeksByMonths, and getVoteSettings match exactly under normalization, that getMembers matches on display name, in-app role and scheduling constraint for every person on both sides, and that no member loses an instrument or PPT eligibility relative to the sheet — the church record knowing more than the retired sheet is reported as a gain rather than a failure, because an added capability cannot invalidate an existing schedule while a removed one can, and that getInitialData carries the same three collections as its individual actions. The two sheet members with no 敬拜部 membership are expected to be reported as one-sided; assert they are the only ones.
- [x] 5.3 Serve the vote actions — castVote, castVoteBulk, getVotes, getVotesByMember, and getVoteSummary — with casting implemented as an upsert keyed by week and person. Verify by casting a changed vote for a member who already voted and asserting exactly one row remains with an advanced update time, and by the parity script matching getVoteSummary for a month with existing votes.
- [x] 5.4 Serve the schedule actions — getSchedule, getMySchedule, saveSchedule, confirmSchedule, and the pre-practice history derived by query — with a week's assignments replaced atomically. Verify by the parity script matching getSchedule and getPrePracticeHistory, and by asserting that a save which violates role cardinality leaves the week's prior assignments intact.
- [x] 5.5 Serve the song and speaker actions — getSongs, getSongsForMonth, getSpeakersForMonth, saveSongs, and submitLeaderSong — returning month maps keyed by week id. publishSongs is not here: it changes no data and exists only to notify, so it ships with the LINE push it consists of. Verify with the parity script matching getSongsForMonth and getSpeakersForMonth for a month that has published songs and an assigned speaker.

## 6. Integrations

- [x] 6.1 Serve LINE login resolved through worship bindings: a code exchange returns the profile with the roster member the worship binding names, or a null member when none exists, and bindLineUser records a new binding. Verify with a seeded member's LINE account that the member is returned, with an unbound test account that the profile comes back with a null member, that binding that account then returns the member on the next sign-in, and that the management system's identity table is untouched in all three cases.
- [x] 6.2 Port LINE push notifications for published song lists, leader submissions, and reminders, addressing approved identities and skipping members without one. Verify by publishing to a week whose roster includes one member with no approved identity and asserting the other members receive the message while the action reports success.
- [x] 6.3 Deliver calendar invitations through the Google Calendar API, implementing the decision to use the Google Calendar API as the calendar's owner — a refresh token obtained once through the consent screen, because a service account cannot invite attendees without the Workspace domain-wide delegation a personal Gmail account cannot configure: practice and service events are created for a week with the roster in the description, using addresses from person records. Verify on a test week that a member whose only role is PPT is an attendee of the service event and absent from the practice event, and that a member with no email is reported as skipped rather than failing the action.
- [x] 6.4 Deliver the scheduled song reminder as a Workers Cron Trigger for the weekly song reminder, sending to that week's 主領 when the third song is missing and the deadline — the Thursday of the preceding week — falls on the current Asia/Taipei date. Verify the two halves separately, because a deadline is always a Thursday and so cannot fall on an arbitrary test day: assert the week selection returns the one week due on a named Thursday and none on a day that is not one, and assert the reminder itself sends once for a missing third song, stays silent with the reason when the third song exists, and stays silent with the reason when no 主領 is assigned. Then invoke the scheduled handler and confirm it runs, reaches the database and selects by the current Asia/Taipei date.
- [x] 6.5 Port the AI scheduling proxy with a runtime-configurable model and a completion budget derived from prompt length. Verify by sending the real multi-week scheduling prompt and confirming the returned plan parses into one entry per requested week, and by pointing the model at a nonexistent identifier and confirming the upstream message surfaces in an error envelope.

## 7. Cutover

- [x] 7.1 Point the SPA at the Worker by supplying its base URL through the build environment, so `src/App.jsx` reaches the new backend without any other change to its api helper. Verify by building with the new value and asserting the bundle contains the Worker URL and no script.google.com URL, then loading the built app and exercising a vote and a schedule save.
- [x] 7.2 Bring production up: apply the migration to the production database, run export, match, import, and seed against it, and deploy the Worker to its production route with secrets bound. Verify with the health check action against the production route and a full parity run reporting no differences before any user traffic is directed at it.
- [ ] 7.3 Decommission the Apps Script backend after one full service cycle by removing `src/Code.gs`, `src/appsscript.json`, and `.clasp.json`, deleting the web app deployment, and updating `README.md` and `.github/workflows/deploy.yml` to describe the Worker backend, its build variables, and the shared database it joins against. Verify that the repository contains no clasp or Apps Script references, that the Pages workflow builds and deploys successfully, and that the sheet is set read-only as the historical archive.
