## 1. Song announcement audience

- [x] 1.1 Record the song audience in worship's own storage with `workers/migrations/0004_song_audience.sql`, implementing the decision that Song recipients outside the roster are recorded as teams, not people, so that The audience is held as data, not as a list of people: a worship-owned table naming management-system teams, seeded with 影音組, with membership still read from the management system at send time rather than copied. Verify by applying the migration to a Neon branch, asserting the seeded row is present, and asserting that a join from the table through the management system's team membership returns 易學仁 and 蘇建勳 and nobody else.
- [x] 1.2 Deliver the rule that The song announcement reaches teams recorded as its audience, so that Each recipient is messaged once and A recipient without a LINE binding is skipped and reported: the week's roster excluding the speaker, unioned with the active members of every audience team, de-duplicated by person and addressed through worship bindings. Verify by publishing a week whose roster overlaps an audience team and asserting one message per distinct person with both audio-visual members among them, by making an audience membership inactive and asserting that person drops out, and by publishing with one recipient unbound and asserting the action succeeds while naming that person as skipped.

## 2. Portal read action

- [x] 2.1 Establish that Viewer identity comes from a server-verified LIFF ID token, implementing the decision that The viewer is identified by a server-verified LIFF ID token: the Worker posts the token to LINE's verify endpoint with worship's channel id and uses the verified subject, never a caller-supplied LINE user id, to find the worship binding. Verify that a token minted for another channel returns an error envelope naming the token and issues no database query, that an absent, malformed, or expired token does the same rather than returning an anonymous view, and that a real token from a bound account resolves to that person.
- [x] 2.2 Serve the portal's single read action so that The portal reads through one action that returns everything it shows, delivering that The portal reports a person's future assignments and that The portal serves viewers who have no worship binding: one action taking the ID token and returning the member or null, the member's assignments grouped by week with their roles, and every service week ordered ascending so the page can change month without asking again. Verify that a member with roles in two weeks gets one entry per week with a multi-role week grouped into a single entry, that a bound member with no assignments at all returns an empty schedule alongside a populated week list, that an unbound account returns a null member with the week list still populated and no error, and that the whole response stays small enough to be worth preferring over per-month requests.
- [x] 2.3 Deliver the rule that The portal presents only published songs, implementing the decision that The portal shows only published songs: confirmed song rows are returned and unconfirmed ones withheld, while a week with nothing publishable stays in the list as awaiting announcement rather than disappearing. Verify on a week holding one confirmed and one unconfirmed song that only the confirmed song is returned, and on a week whose songs are all unconfirmed and a week with no song rows that both still appear in the response with no songs.

## 3. Portal page

- [x] 3.1 Publish the portal beside the existing app, implementing the decision that The portal ships as a second page of the same Vite build and delivering that The portal is published beside the existing app: a second Vite input inlined by the existing single-file plugin, carrying the LIFF id as a build variable, with the existing application page and its LINE Login flow unchanged. Verify by building and asserting both pages are present in `dist`, then serving `dist` and loading each page in a browser, asserting no uncaught console error on either and that the existing app still renders its login screen.
- [x] 3.2 Deliver the portal's screen so that The portal is a reader and The portal states when it is opened outside LINE: one card per service week in the visual language of the full app's 我的班表 screen, each carrying that week's speaker, its published songs and the roles the viewer holds that week, with one month shown at a time steppable backwards and forwards, weeks still to come listed before past ones under a completed heading, the unbound case naming the full web app as the place to link a LINE account, and no control that writes. Verify by loading the page outside LINE and asserting it says it must be opened from LINE with no uncaught error, by exercising every control the page presents and asserting every request issued is a read, by asserting a month change issues no request, and by rendering the serving, bound-but-never-scheduled, and unbound cases against real data.

## 4. Release

- [x] 4.1 Bring the portal up in LINE, implementing the decision that The portal registers under worship's own LINE Login channel: apply the migration and deploy the Worker and the site, then register the LIFF app under channel 2009964527 with the `profile` and `openid` scopes granted, pointing at the published portal page, and add the rich menu entry on the worship official account. Verify by opening the rich menu entry on a bound member's phone and reading their own upcoming assignments, and on an account with no worship binding and reading the songs with the unlinked notice.
- [x] 4.2 Describe the portal in `README.md` so an operator can rebuild and redeploy it without reading the code: the new build variable and where its value comes from, the LIFF app and the channel it belongs to, the rich menu entry, and how to add a team to the song audience. Verify by following the README's own steps from a clean checkout to a built `dist` containing both pages, and by asserting the described SQL adds a team the announcement then reaches.
