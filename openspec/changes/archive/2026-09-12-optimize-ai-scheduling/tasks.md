## 1. Measure before changing

- [x] 1.1 Report what a period's prompt costs with `scripts/prompt-budget.mjs`, delivering a budget check that fails loudly: it builds the prompt for a given set of months, prints the character count, the estimated prompt tokens, the reservation left for the answer and the capacity spare above what an answer needs, and exits non-zero when the prompt fits the per-minute token budget with headroom is violated — under 1500 tokens spare. Verify by running it for the current nine-week period and asserting it reports today's 7771 characters, 2943 tokens reserved and 943 spare, below the threshold, and exits non-zero — the baseline this change has to move.

## 2. Rebuild the prompt

- [x] 2.1 Compose availability as one compact table so each fact appears once in the scheduling prompt: a numbered week legend printed once, then one line per member carrying their instruments, their constraint when set, and the indices of the weeks they are available — replacing both the padded grid and the per-week member list. Verify with the budget script that the same nine-week period now produces a prompt at most half of 7771 characters, and by reading the generated prompt to confirm each member appears on exactly one line and no line is padded for alignment.
- [x] 2.2 Ensure only schedulable instruments are offered to the model, by deriving the role vocabulary from the roster the API returns, excluding PPT and 練前預備 because each is assigned by its own pass, implementing the decision on role vocabulary derived from the roster. Because the model is no longer asked for PPT, the eligibility pass gains the ability to fill an empty slot as well as correct a wrong one, applying the same rule. Verify by building a prompt from a roster containing a position not in the old hard-coded list and asserting it appears as a schedulable role, that neither PPT nor 練前預備 appears in the vocabulary or the prompt, and that a plan with no PPT for a week comes out of the pass with one assigned.
- [x] 2.3 State the rotation rules once as constraints rather than as a tutorial, implementing the decision that rules the client enforces are stated once, not taught, and stop requesting per-week prose so that no output is requested that nothing reads. Verify by asserting the rules section is shorter than the 807 characters it occupies today while still naming the monthly caps, the consecutive-week rule and the PPT rule, and that neither the prompt nor the response schema mentions an explanation field.

## 3. Ask for structured output

- [x] 3.1 Request JSON so that the model answers with schema-valid JSON, implementing the decision on structured JSON output instead of a text format: the client supplies the role vocabulary and the requested weeks, and the proxy sends a response format naming a schema whose top level is a list of weeks, each with its date and role-and-members entries. Measure whether the reasoning effort can rise now that the prompt is smaller, and record the answer in the decision on reasoning effort stays low, for a reason the budget hid. Verify by planning the nine-week period and asserting the response parses as JSON satisfying the schema on the first attempt, with no text parsing involved.
- [x] 3.2 Validate a plan before it is shown so that an invalid plan is rejected, never partially accepted: check every requested week is present exactly once, every role is within the vocabulary, every name comes from that week's availability list, and no role carries more members than it seats, replacing the two text parsers with this one path. Collect every fault rather than the first and hand them back to the planner, implementing the decision on a repair loop, because one attempt is not enough — one correction attempt before giving up. Split a multi-month period so that a period is planned one month at a time, implementing the decision on one month per request: each is a smaller problem carrying a self-contained monthly cap. Size the completion reservation so a call and its correction fit inside the same per-minute budget. Verify with three crafted answers — one missing a week, one naming a member who is unavailable that week, and one putting three people in a two-seat role — asserting each is rejected with a message naming the week and the reason, that a valid answer passes, and by measuring repeated runs against the live model and recording the rate rather than asserting one: on the free tier a nine-week period comes back usable in roughly one run in three, the same rate the retired prompt achieved when its answers are held to the same check, and the difference is that a failure is now reported instead of shipped.

## 4. Compare and switch

- [x] 4.1 Confirm post-processing guarantees are unchanged by planning the same nine-week period with the retired prompt and the new one and comparing the results: weeks covered, names falling outside availability, and how many assignments the monthly-cap, consecutive-week and PPT passes rewrite. Verify by recording both runs. The new prompt covers all nine weeks with no name outside availability, and the passes rewrite 11 of its assignments. The retired prompt cannot be compared on equal footing — two of four runs returned only two and six of the nine weeks — so the rewrite counts are recorded rather than asserted against each other; more rewriting is not a correctness regression, because the passes decide the final answer either way, but it does mean the first draft sits further from the rotation ideal now that the caps are stated as a preference rather than a rule.
- [x] 4.2 Leave the planner within its budget by re-running the budget script once the prompt is final, and record the measured spare capacity in the change's design notes. Verify that the script exits zero for the nine-week period and reports at least 1500 tokens spare.
