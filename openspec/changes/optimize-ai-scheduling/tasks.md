## 1. Measure before changing

- [ ] 1.1 Report what a period's prompt costs with `scripts/prompt-budget.mjs`, delivering a budget check that fails loudly: it builds the prompt for a given set of months, prints the character count, the estimated prompt tokens, the reserved output and the resulting headroom against the configured per-minute budget, and exits non-zero when the prompt fits the per-minute token budget with headroom is violated — under 1500 tokens spare. Verify by running it for the current nine-week period and asserting it reports today's 7771 characters with headroom below the threshold and exits non-zero, which is the baseline this change has to move.

## 2. Rebuild the prompt

- [ ] 2.1 Compose availability as one compact table so each fact appears once in the scheduling prompt: a numbered week legend printed once, then one line per member carrying their instruments, their constraint when set, and the indices of the weeks they are available — replacing both the padded grid and the per-week member list. Verify with the budget script that the same nine-week period now produces a prompt at most half of 7771 characters, and by reading the generated prompt to confirm each member appears on exactly one line and no line is padded for alignment.
- [ ] 2.2 Ensure only schedulable instruments are offered to the model, by deriving the role vocabulary from the roster the API returns, excluding PPT and 練前預備 because each is assigned by its own pass, implementing the decision on role vocabulary derived from the roster. Verify by building a prompt from a roster containing a position not in the old hard-coded list and asserting it appears as a schedulable role, and that neither PPT nor 練前預備 appears in the vocabulary.
- [ ] 2.3 State the rotation rules once as constraints rather than as a tutorial, implementing the decision that rules the client enforces are stated once, not taught, and stop requesting per-week prose so that no output is requested that nothing reads. Verify by asserting the rules section is shorter than the 807 characters it occupies today while still naming the monthly caps, the consecutive-week rule and the PPT rule, and that neither the prompt nor the response schema mentions an explanation field.

## 3. Ask for structured output

- [ ] 3.1 Request JSON so that the model answers with schema-valid JSON, implementing the decision on structured JSON output instead of a text format: the client supplies the role vocabulary and the requested weeks, and the proxy sends a response format naming a schema whose top level is a list of weeks, each with its date and role-and-members entries. Raise the reasoning effort back to medium as the decision on reasoning effort returns to medium describes, now that the smaller prompt affords it. Verify by planning the nine-week period and asserting the response parses as JSON satisfying the schema on the first attempt, with no text parsing involved.
- [ ] 3.2 Validate a plan before it is shown so that an invalid plan is rejected, never partially accepted: check every requested week is present exactly once, every role is within the vocabulary, every name comes from that week's availability list, and no role carries more members than it seats, replacing the two text parsers with this one path. Verify with three crafted answers — one missing a week, one naming a member who is unavailable that week, and one putting three people in a two-seat role — asserting each is rejected with a message naming the week and the reason, and that a valid answer passes.

## 4. Compare and switch

- [ ] 4.1 Confirm post-processing guarantees are unchanged by planning the same nine-week period with the retired prompt and the new one and comparing the results: weeks covered, names falling outside availability, and how many assignments the monthly-cap, consecutive-week and PPT passes rewrite. Verify by recording both runs and asserting the new prompt covers all nine weeks, produces no name outside availability, and does not increase the number of assignments the enforcement passes have to rewrite.
- [ ] 4.2 Leave the planner within its budget by re-running the budget script after the reasoning effort is raised, and record the measured headroom in the change's design notes. Verify that the script exits zero for the nine-week period and reports at least 1500 tokens of headroom.
